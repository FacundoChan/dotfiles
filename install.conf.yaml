- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.vimrc: vimrc
    ~/.gitignore_global:

- shell:
    - [git submodule update --init --recursive, Installing submodules]
